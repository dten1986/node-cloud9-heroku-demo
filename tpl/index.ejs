<!DOCTYPE html>
<html>
<head>
	<script src="/bower/bower_components/jquery/jquery.js"></script>
	<script src="/bower/bower_components/underscore/underscore.js"></script>
	<link rel="stylesheet" type="text/css" href="css/style.css" />
</head>
<body>
	<h1>The List</h1>
<!--
	<% block('header', "Test header2") %>
	<h2><%=blocks.header %></h2>
-->
    <ul class="list-wrapper"></ul>
	<form class="form">
		<input name="input" class="input" placeholder="What do you wish?.." />
		<button type="submit" class="submit">Submit</button>
	</form>
	
	<script>
	    function getList() {
        	$.getJSON('/items/list', function(data) {
        	    $('.list-wrapper').empty();
    	        $(data).each(function(num) {
    	            var doneState = '',
    	                checkState = '';
    	            if (this.done == true) {
    	                doneState = 'class="m-done"';
    	                checkState = 'checked';
    	            };
    	            
    	            var item = '<li id="' + this.id + '" class="item_no_' + num + '">' +
                                    '<input type="checkbox" class="checkbox" ' + checkState + '>' +
                                    '<span ' + doneState + '>' + this.date + '</span>' +
                                    '<span class="text" ' + doneState + '>' + this.text + '</span>' +
                                    '<button class="remove">Remove</button>' +
                                '</li>';
                    $(item).appendTo('.list-wrapper');   
    	        });
    	    });
	    };
	    
	    function itemIdSend(address, itemID) {
	        $.ajax({
		        url: address,
		        method: 'POST',
		        data: {id: itemID},
		        complete: function() {
				    getList();
				}
		   });
	    };
	    
	    getList();
		  
		$('.form').on('submit', function() {
			$.ajax({
				url: '/items/add',
				method: 'POST',
				data: $(this).serialize(),
				complete: function() {
				    getList();
				}
			});
			
			return false;
		});
		
	    $('.list-wrapper').on('click', '.remove', function() {
	        var remID = $(this).parent().attr('id');
		    itemIdSend('/items/remove/', remID);
	    });
	   
	    $('.list-wrapper').on('click', '.checkbox', function() {
	        var $this = $(this);
	        var itemID = $(this).parent().attr('id');
	        if ($this.is(':checked')) {
	            $this.siblings().addClass('m-done');
	        } else {
	            $this.siblings().removeClass('m-done');
	        };
	        itemIdSend('/items/done/', itemID);
	    });
	   
	</script>
</body>
</html>
